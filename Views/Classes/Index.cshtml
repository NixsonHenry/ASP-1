@model IEnumerable<PAS.Models.Classe>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Niveau)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Section)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AnneeAcademique)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Niveau)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Section)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AnneeAcademique.AnneeAcademiqueId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ClasseId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ClasseId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ClasseId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
